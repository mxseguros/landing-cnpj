# --- Segredos ---
# Tudo que é .env fica fora do git. A exceção é .env.example, que só tem
# nomes de variáveis e serve de documentação.
.env
.env.*
!.env.example

# --- Dependências e build ---
node_modules/
.next/
out/
build/
dist/

# --- Vercel ---
.vercel

# --- Ferramentas ---
*.tsbuildinfo
next-env.d.ts
coverage/
.turbo/

# --- Sistema ---
.DS_Store
Thumbs.db
*.log

.vercel

# --- Originais e temporários de escritório ---
# Foto original enviada pela MX (15 MB). O que o site usa é o recorte em
# public/hero/agro-lavoura-irrigada.jpg; o original fica no disco, fora do git.
/agriculture-healthy-food.jpg
# Arquivos de lock do Office, criados enquanto um .docx/.pptx está aberto.
~$*
